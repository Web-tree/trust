logging:
 level:
    com.datastax.driver.core.QueryLogger.NORMAL: DEBUG
    com.datastax.driver.core.QueryLogger.SLOW: TRACE
    org.springframework.security.authentication.dao: DEBUG

spring:
  profiles:
    include: cassandra-active
  data:
    cassandra:
      port: 9142
