#Default profile
server:
  port: 9000
spring:
  profiles:
    active: dev
  data:
    cassandra:
      keyspace-name: trust_main
      port: 9042
      contact-points: localhost
jwt:
  header: Authorization
  secret: mySecret
  expiration: 604800
  route:
    authentication:
      path: token/new
      refresh: refresh
frontend:
  origins: https://.webt-ree.org